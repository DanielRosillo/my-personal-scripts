flutter clean

flutter build apk --release --obfuscate --split-debug-info=build/debug_info

keytool -genkey -v -keystore ~/upload-keystore.jks -keyalg RSA \
        -keysize 2048 -validity 10000 -alias upload


flutter pub run rename setBundleId --targets android --value "com.duxgor25.systems.contacts_extractor"

flutter pub run rename setAppName --targets ios,android --value "Contacts Extractor by Duxgor25"